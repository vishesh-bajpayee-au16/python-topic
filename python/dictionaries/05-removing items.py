# Removing Items

thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}

thisdict.pop("model")
print(thisdict)

# popiten() removes the last item from list
thisdict.popitem()
print(thisdict)

# del keyword is used t odelete a specific key name

# clear() empties the dictionalry

thisdict.clear()
